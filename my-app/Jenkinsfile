pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Run Shell Script') {
      steps {
        sh '''
        chmod +x /my-app/app.sh
        ./my-app/app.sh
        '''
      }
    }
  }
}

